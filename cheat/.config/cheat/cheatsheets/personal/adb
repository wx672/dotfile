# wireless
1. turn off clash
2. wireless debugging -> pair device with pairing code

```
# adb version should be greater than 30.0  
adb pair ip:port paircode 
adb connect ip:anotherport
```

# backup restore
adb backup -apk -shared -all -f backup-$(Date).ab
adb restore backup.ab
